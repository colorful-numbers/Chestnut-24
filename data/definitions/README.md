# Definitions

Each markdown file in this folder is parsed into the `/defn` page and local definition tooltips.

Use this structure:

```md
# Term Title

First paragraph. This paragraph becomes the short tooltip description.

Longer explanation can follow.
```

Use `aliases` in frontmatter when a term has multiple labels:

```md
---
aliases: ["Miracle"]
---
# 奇迹
```
